{
  "name": "websocket",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun esbuild src/index.ts --bundle --platform=node --outdir=dist",
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@repo/typescript-config": "*",
    "@repo/db": "*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}